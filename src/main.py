import execute as ex
import pygame as pg
import sys

if __name__ == "__main__": 

    # Run the application 
    ex.run_application() 

    # Quit pygame and exit the system
    pg.quit()
    sys.exit()